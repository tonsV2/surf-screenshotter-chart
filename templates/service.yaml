apiVersion: v1
kind: Service
metadata:
  name: {{ include "surf-screenshot.fullname" . }}
  labels:
    {{- include "surf-screenshot.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "surf-screenshot.selectorLabels" . | nindent 4 }}
