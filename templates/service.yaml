apiVersion: v1
kind: Service
metadata:
  name: {{ include "surf-screenshot-chart.fullname" . }}
  labels:
    {{- include "surf-screenshot-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "surf-screenshot-chart.selectorLabels" . | nindent 4 }}
