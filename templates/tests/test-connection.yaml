apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "surf-screenshot-chart.fullname" . }}-test-connection"
  labels:
    {{- include "surf-screenshot-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "surf-screenshot-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
