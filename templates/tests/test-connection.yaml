apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "surf-screenshot.fullname" . }}-test-connection"
  labels:
    {{- include "surf-screenshot.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "surf-screenshot.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
